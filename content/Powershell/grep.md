# PowerShell's grep alternative

PowerShell has a command that provides similar function to grep in Linux.

## Command

```PowerShell
Select-String -Path "file.txt" -Pattern 'string' | Out-File results.txt
```

## More Information
